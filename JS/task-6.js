let input;
const numbers = [];
let total = 0;

do {
    input = prompt('Введи число');
    total += Number(input);
} while(input !== null);

alert(`Общая сумма чисел равна ${total}`);

for (let number of numbers) {
    sum += n
}